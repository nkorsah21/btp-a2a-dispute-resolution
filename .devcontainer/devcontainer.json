{
  "name": "Existing Docker Compose (Extend)",

  "dockerComposeFile": [
    "../docker-compose.yml",
    "docker-compose.yml"
  ],

  "service": "btp-a2a-dispute-resolution",

  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  "remoteUser": "node",

  "postCreateCommand": "sudo chown -R node:node . && npm install"
}
